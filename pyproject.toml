[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mooze"
version = "0.1.0"
dependencies = [
    "textual",
    "yt-dlp"
]

[project.scripts]
mooze = "mooze.main:start"  # 👈 CHANGED: Points to the inner package folder